/**
 * @defgroup io IO
 * @brief IO components of the CoffeeEngine.
 * @{
 */

#pragma once

namespace Coffee {

    /**
     * @enum ResourceFormat
     * @brief Enumeration of resource formats.
     */
    enum class ResourceFormat
    {
        Binary, ///< Binary format
        JSON    ///< JSON format
    };

}

/** @} */